node_modules
dist/
coverage/
.env
.env.local
.env.production
.DS_Store
*.log

# WhatsApp Flow RSA keys — NEVER commit private keys
flow-keys/
*.pem
supabase/.temp/
.vercel
docs/private/
playwright-report/
test-results/
.playwright/

# AI assistant working directories — local session artifacts, not shipping code
.agents/
# Claude Code: keep personal/local state out, but track the shared SessionStart
# hook so every session provisions the database-test stack the same way.
.claude/*
!.claude/hooks/
!.claude/settings.json
.claude/settings.local.json
.claude-scratch/
.codex/
.firecrawl/
.playwright-cli/
.playwright-mcp/
.superpowers/
.zed/

# Local design-alignment working directory — not shipping code
design-alignment/
.env*

# design-sync: staged converter scripts + regenerated build output + machine state
# NOTE: .design-sync/ itself stays TRACKED — config.json, NOTES.md,
# conventions.md, previews/ and docs-stubs/ are hand-maintained source. The
# sync tool never writes there (see .ds-sync/lib/previews.mjs); everything it
# generates goes under .design-sync/.cache/.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Launch-kit marketing collateral — regenerate with
# `node scripts/generate-launch-kit.mjs` / `generate-premium-launch-kit.mjs`.
# Nothing in the app or the build reads these.
outputs/
pr-gate-results/
