# Human merge-control live probe

This temporary, documentation-only file exists to exercise the repository's
human merge control after its bootstrap merge.

It changes no product behaviour, database schema, deployment configuration or
customer data. The probe pull request must remain unmerged and finish on
`HOLD` before it is closed.

This harmless follow-up line exists solely to prove that a new commit invalidates
an approval recorded for the previous head SHA.
