// Moved to src/components/ui/StatusPill.jsx so the conversation-state pill can
// be shared beyond the inbox. Re-exported here so existing relative imports
// (and tests) keep working unchanged.
export { StatusPill } from "../../ui/StatusPill.jsx";
