# Keep the website's peer dependencies in its independently generated lockfile.
# The repository root uses legacy-peer-deps for the bookings application, but
# applying that compatibility setting here makes Dependabot omit required peers.
legacy-peer-deps=false
